# Imprezy klubowe — kierownik rezerwuje sprzęt — podsumowanie wdrożenia

Plan: [`04.09_imprezy_klubowe_rezerwacja_PLAN.md`](./04.09_imprezy_klubowe_rezerwacja_PLAN.md).
Status: **wdrożone na prod (morzkulc-e9df7), w pełni zweryfikowane na żywo — łącznie z edycją.**
Invoker `updateBundleGearReservationItems` przełączony na Public access przez użytkownika,
edycja (dodaj + usuń pozycję) potwierdzona: `POST /api/gear/reservations/update-items` → 200,
zmiana przetrwała przeładowanie strony.

## Co działa (zweryfikowane live na prod)

| Funkcja | Status |
|---|---|
| Organizator (5 klubów) + kierownik — formularz zgłoszenia imprezy | ✅ |
| Organizator/kierownik — sync dwukierunkowy z arkuszem (kolumny "Organizator"/"Kierownik") | ✅ |
| Aktywacja przywileju kierownika dopiero po zatwierdzeniu imprezy | ✅ |
| Blokada: 1 aktywny kierownik na raz (przy zatwierdzeniu i przy korekcie w arkuszu) | ✅ |
| Kafelek "Sprzęt na imprezę" (zielony, home) — widoczny tylko dla aktywnego kierownika | ✅ |
| Rezerwacja bezpłatna, bez limitu ilości, daty = daty imprezy (bez kalendarza) | ✅ |
| Konflikt terminów i sprawność sprzętu — NIGDY nie pomijane nawet w trybie klubowym | ✅ |
| Widoczność dla wszystkich: "Impreza klubowa — kierownik: {imię nazwisko}" na liście "Więcej" i w tooltipach | ✅ |
| Ikonki 5 klubów na liście imprez (desktop) | ✅ |
| **Edycja listy przedmiotów już złożonej rezerwacji** (dodaj/usuń pozycję później) | ✅ |
| Przyciski "Rezerwuj"/"Anuluj" na górze ekranu, przed przewijaną listą (mobile fix) | ✅ |

## Runda 2 (dzisiejszy feedback) — co zmieniono

Użytkownik zgłosił: na mobile nie widać przycisku zapisu (był na dole, pod całą przewijaną
listą) + brak przycisku anulowania + potrzeba edycji już złożonej rezerwacji (potrzeby na
imprezę się zmieniają).

1. **Przyciski na górze.** `renderClubEventBulkView` (`public/modules/gear_module.js`) —
   "Rezerwuj" + "Anuluj" przeniesione na sam początek karty, przed zakładkami kategorii i listą.
   CSS: `.clubEventActionsTop` w `public/styles/gear.css` (przyciski równej szerokości, bez
   zawijania na wąskim ekranie).
2. **Edycja listy — nowa trwała rezerwacja per kierownik per impreza**, nie kolejne osobne
   zapisy:
   - Przy wejściu na ekran front pobiera `/api/gear/my-reservations` i szuka WŁASNEJ aktywnej
     rezerwacji o dokładnie takich samych datach jak aktywna impreza (proxy dla "należy do tej
     imprezy" — front nigdy nie zna właściwego `eventId`, patrz `club_badges.js`). Jeśli
     znaleziono → checkboxy pre-zaznaczone, "Rezerwuj" od tej pory EDYTUJE tę rezerwację.
   - **Naprawiony bug po drodze**: pozycje z WŁASNEJ już złożonej rezerwacji serwer i tak
     zgłaszał jako "zajęte" (bo kolidują same ze sobą) → checkbox byłby zablokowany, nie dałoby
     się odznaczyć. Fix: `isAvail = it.isAvailableForRange !== false || checked`.
   - Nowy endpoint backendowy **`POST /api/gear/reservations/update-items`**
     (`updateBundleReservationItems()` w `gear_bundle_service.ts`, handler
     `gearBundleReservationUpdateItemsHandler.ts`, funkcja Cloud
     `updateBundleGearReservationItems`) — podmienia PEŁNĄ listę przedmiotów istniejącej
     rezerwacji na imprezę klubową. Zasady: działa WYŁĄCZNIE na rezerwacjach z `eventId`; wymaga
     wciąż być aktywnym kierownikiem TEJ SAMEJ imprezy; limit ilości pomijany (jak przy
     tworzeniu); konflikt terminów (z wyłączeniem samej siebie) i sprawność przedmiotów NIGDY nie
     pomijane; przelicza i koryguje wyświetlaną kwotę "waived" w `godzinki_ledger` jeśli liczba
     kajaków się zmieniła.
   - Frontend śledzi stan "dirty" (porównanie z ostatnio zapisanym stanem) — "Rezerwuj"
     wyłączony gdy nic się nie zmieniło, tekst podsumowania rozróżnia "Zapisano" /
     "Niezapisane zmiany" / "Wybrano" (nowa rezerwacja).
   - "Anuluj" = wyjście do strony głównej; jeśli są niezapisane zmiany, prosi o potwierdzenie
     (`window.confirm`).
3. **Testy**: `tests/test_bundle_reservations.py` — nowa klasa `TestScenarioClubEventItemsEdit`
   (8 testów: dodaj+usuń, konflikt, self-exclusion, sprawność, utrata roli kierownika, zwykła
   rezerwacja bez `eventId`, pusta lista, brak uprawnień właściciela). Cały plik: **119/119
   passing**. `functions` (vitest): **123/124** (1 fail pre-istniejący, niezwiązany —
   `boardDoesNotPay` w `gear_core.test.ts`).

## DO ZROBIENIA

1. ~~Ustawić Public access na `updateBundleGearReservationItems`~~ — **zrobione przez
   użytkownika, potwierdzone działanie na żywo (200, zmiana przetrwała reload).**
2. **Rzeczywisty test na telefonie** — `resize_window` w tym środowisku nie zmienia realnego
   viewportu (znane ograniczenie, patrz pamięć projektu), więc nowy układ przycisków na górze
   nie został zweryfikowany wizualnie na prawdziwym mobile w tej sesji, tylko logicznie
   (kolejność w DOM + CSS).

## Efekt uboczny testów na żywo

W ramach Twojego aktywnego kierownictwa imprezy **02.10.2026 – 04.10.2026** istnieje testowa
rezerwacja: kajaki **Pyranha Ammo M (nr 11)** + **Eskimo Salto (nr 15)**, rzutki **HF (nr 10)** +
**HF (nr 11)** — bezpłatnie, oznaczone jako impreza klubowa. Część z tego dodał w ramach własnego
testu edycji użytkownik. Zostawiona jak jest (żadna z pozycji nie została anulowana) — daj znać,
czy ją wyczyścić, czy zostawić.

## Runda 3 (dzisiejszy feedback #2) — ergonomia edycji/usuwania

Feedback: mylące, że przycisk zawsze mówi "Rezerwuj" (nawet do edycji), brak sposobu na pełne
usunięcie rezerwacji z tego ekranu (odznaczenie wszystkiego i zapis blokowany — `no_items`),
tytuł ekranu nie mówi na jaką imprezę, podsumowanie nie pokazywało KONKRETNYCH pozycji
(tylko liczby), tylko per kategoria aktualnie widoczna.

1. **Tytuł ekranu z nazwą imprezy**: `activeKierownikRange` w odpowiedzi `/api/register`
   (`registerUserHandler.ts`, `resolveKierownikSummary`) niesie teraz też `name` (nazwa imprezy —
   WYŁĄCZNIE do własnego ekranu kierownika, nie zmienia nic w widoczności dla innych
   użytkowników, która nadal nie pokazuje nazwy imprezy). Front: `Sprzęt na „{nazwa}"`.
2. **Przycisk "Rezerwuj" → "Edytuj"** automatycznie, gdy `existingReservationId` jest ustawiony.
3. **Nowy przycisk "Usuń rezerwację"** (duży, wyśrodkowany, czerwony, pod górnym paskiem
   Edytuj/Anuluj) — woła istniejący `/api/gear/reservations/cancel` bezpośrednio z tego ekranu,
   z potwierdzeniem. Widoczny tylko gdy jest co usunąć. Naprawia realny problem: bez tego
   przycisku pełne usunięcie rezerwacji z tego ekranu było niemożliwe (zapis pustej listy
   zablokowany celowo — `no_items`).
4. **Podsumowanie podzielone na kategorie sprzętu** (kajaki/wiosła/kamizelki/kaski/fartuchy/
   rzutki), z konkretnymi nazwami+numerami pozycji, nie tylko liczbami — do wygodnej weryfikacji
   całej listy bez przełączania zakładek. Etykiety cache'owane (`labelCache`) z dwóch źródeł:
   już zapisanej rezerwacji (`itemLabel`/`itemNumber` zdenormalizowane w dokumencie) i list
   dostępności odwiedzonych zakładek — bez potrzeby eager-loadingu wszystkich 6 kategorii.

Zweryfikowane live: tytuł z prawdziwą nazwą imprezy ("Przerodziny 2026"), etykieta "Edytuj",
przycisk "Usuń rezerwację" poprawnie widoczny/ukryty, podsumowanie per kategoria zgodne z
rzeczywistym stanem zaznaczeń na każdej zakładce. Sam klik "Usuń rezerwację" NIE został
przetestowany na żywo (dane mogły być realne/w użyciu przez użytkownika równolegle) — logika
woła niezmieniony, długo działający `cancelReservation()`, ryzyko niskie.

## Runda 4 (dzisiejszy feedback #3) — nazwa imprezy widoczna też dla innych użytkowników

Feedback: skoro kierownik widzi już nazwę imprezy na swoim ekranie, niech widzą ją też inni
użytkownicy przeglądający sprzęt (wcześniej świadomie ukryta — "Impreza klubowa — kierownik: X"
bez nazwy). Zmieniona decyzja projektowa.

- `getKayakReservationsHandler.ts` (żywi zarówno per-item "Więcej", jak i tooltips kalendarza
  dat) — batch-fetch nazwy imprezy po `eventId` zapisanym na rezerwacji, analogicznie do
  istniejącego batch-fetchu nazwisk użytkowników. Nowe pole `eventName` w odpowiedzi.
- `gear_module.js` (dwa miejsca: `renderReservationsSimple` i `loadOccupiedRanges`) i
  `my_reservations_module.js` (`loadOccupiedRangesForReservation`) — tekst zmieniony na
  "Impreza klubowa: {nazwa} — kierownik: {osoba}" (z fallbackiem bez nazwy, gdyby z jakiegoś
  powodu nie udało się jej odczytać).

Zweryfikowane live na kajaku Aquarius SeaLion (nr 1): "Zajęty: 01.10.2026 – 05.10.2026 / Impreza
klubowa: Przerodziny 2026 ** 🎉 — kierownik: Krzysztof Świtek." Stan testowy przywrócony do
poprzedniego (Rzutki — 1 szt., HF nr 10) po weryfikacji.

## Runda 5 (dzisiejszy feedback #4) — uproszczony tekst widoczności

Feedback: format z rundy 4 ("Impreza klubowa: {nazwa} — kierownik: X") jednak za długi/za dużo
informacji — ma być WYŁĄCZNIE `Rezerwacja "{nazwa imprezy}"`, bez słowa "Impreza klubowa" i bez
kierownika w ogóle.

Zmiana tylko w 3 miejscach frontu (backend z rundy 4 — `eventName` w odpowiedzi
`getKayakReservationsHandler.ts` — bez zmian, wystarczał):
- `gear_module.js`: `renderReservationsSimple` (lista "Więcej") i `loadOccupiedRanges`
  (tooltip kalendarza) → `Rezerwacja „{nazwa}”` (fallback `Rezerwacja (impreza klubowa)`, gdyby
  z jakiegoś powodu nazwy nie udało się odczytać).
- `my_reservations_module.js`: `loadOccupiedRangesForReservation` → to samo.

Zweryfikowane live na kajaku Eskimo Diablo (nr 13): "Zajęty: 01.10.2026 – 05.10.2026 / Rezerwacja
„Przerodziny 2026 ** 🎉”." Stan testowy przywrócony (Rzutki — 1 szt., HF nr 10).

## Runda 6 (dzisiejszy feedback #5) — dwa błędy w widoku sprzętu

Feedback: (1) rzutki na ogólnej liście "Sprzęt" bez numerów, (2) w widoku rezerwacji na imprezę
rzutka pokazywała się jako zajęta mimo usunięcia jej z rezerwacji.

**Przyczyna #2 (stan "zajęty" nie odświeża się)**: `renderClubEventBulkView` wczytuje
dostępność KAŻDEJ kategorii TYLKO RAZ na wizytę ekranu (`loadCategory` ma `if (s.loaded) return`
— świadomie, żeby nie odpytywać API co przełączenie zakładki). Po zapisaniu/edycji/usunięciu
rezerwacji flaga `isAvailableForRange` w już wczytanych kategoriach stawała się nieaktualna —
jeśli pozycja BYŁA zaznaczona (więc override `checked→isAvail` ją ukrywał) i potem została
odznaczona+zapisana, cache nadal pamiętał `isAvailableForRange:false` sprzed usunięcia →
checkbox wracał jako zablokowany "zajęty". Dotyczyło WSZYSTKICH 6 kategorii (nie tylko rzutek) —
jeden centralny fix. Naprawa: nowa funkcja `invalidateAvailabilityAndRefresh()` czyści cache
wszystkich kategorii i odświeża aktywną zakładkę po KAŻDYM udanym zapisie/edycji/usunięciu.
Zweryfikowane live: zapisano HF10+HF11, usunięto HF10 przez Edytuj → HF10 natychmiast (bez
przeładowania strony) pokazał się jako wolny.

**Przyczyna #1 (brak numerów) — głębsza**: `renderGenericGearCard` (jedyna funkcja renderująca
rzutki i fartuchy — pozostałe kategorie mają własne dedykowane funkcje) używała WYŁĄCZNIE
martwego layoutu `.gearInlineMeta`/`.gearCardActions` (`display:none` bazowo, nigdy niewłączane
z powrotem — patrz [[project_gear_card_dead_layout]]), więc oprócz numeru niewidoczny był też
CAŁY przycisk "Rezerwuj" i podgląd rezerwacji na tej ogólnej liście dla obu kategorii. Naprawa:
przepisana na żywy wzorzec (`.gearNrColorMobile` + `.gearMiniBar`, jak reszta kategorii), zgodnie
z życzeniem użytkownika bez sekcji zdjęć (rzutki/fartuchy nie mają prawdziwych zdjęć) — tytuł
karty to teraz po prostu "{producent} {numer}", np. "HF 14". CSS: nowa klasa `.gearCardNoPhoto`
kolapsująca kolumnę na zdjęcie. Przy okazji naprawiony pokrewny bug: podgląd "..." zawsze pytał
API o kategorię "kayaks" niezależnie od realnej kategorii karty (`loadAndRenderReservations` bez
parametru category) — podgląd rezerwacji dla wioseł/kamizelek/kasków/rzutek/fartuchów zawsze
wracał pusty; teraz czyta kategorię z przycisku Rezerwuj tej samej karty. Zweryfikowane live na
rzutkach (numery+Rezerwuj+podgląd działają) i kajaku (brak regresji).

## Runda 7 (dzisiejszy feedback #6) — bez przycisku "Więcej" dla rzutek/fartuchów

Feedback: skoro te kategorie nie mają zdjęć, przycisk "Więcej"/rozwijanie też jest zbędne —
wszystko ma być widoczne od razu, max dwa wiersze.

`renderGenericGearCard` uproszczona jeszcze bardziej: usunięty przycisk "..." i cały panel
`<details>` (statyczne wiersze Kategoria/Typ/Kolor/... + podgląd rezerwacji). Teraz dokładnie
dwa wiersze: (1) tytuł "{producent} {numer}" + ulubione + odznaki typ/rozmiar/status/basen,
(2) pozostałe dane tekstem — kolor/uwagi, tylko gdy niepuste — po lewej, "Rezerwuj" po prawej
(nowa klasa `.gearMiniMeta`). Usunięta też martwa po tej zmianie funkcja
`buildGenericGearDetailsRows`. Zweryfikowane live na rzutkach i fartuchach — kompaktowe karty
jednowierszowe (dane koloru/uwag w tej partii danych akurat puste, więc drugi wiersz pusty poza
Rezerwuj — poprawne, nic nie jest ukryte).

## Runda 8 (05.09.2026) — mechanizm godzinkowy CAŁKOWICIE pomijany dla imprez klubowych

Feedback: wpisy "Impreza klubowa -6h / zwrócono" w historii godzinek są mylące i ryzykowne —
kierownik bez godzinek mógłby w teorii nie móc zarezerwować sprzętu. Decyzja: dla rezerwacji na
imprezę klubową (tworzenie I edycja listy) mechanizm godzinkowy ma być pomijany W CAŁOŚCI, nie
"zwalniany" — zero wpisów w `godzinki_ledger`, żadnej zależności od stanu pul.

- `gear_bundle_service.ts`: `feeExempt` już nie zawiera `Boolean(clubEvent)` — impreza klubowa
  obsługiwana całkiem osobno. `costHours = clubEvent ? 0 : quoteKayaksCostHours(...)`,
  `godzinkiVars`/zapis do `godzinki_ledger` (`writeWaivedSpendInTx`/`deductHoursInTx`) w całości
  pominięte gdy `clubEvent`. `updateBundleReservationItems` (dotyczy WYŁĄCZNIE rezerwacji na
  imprezę klubową) uproszczona — usunięty cały odczyt/zapis `godzinki_ledger` i przeliczanie
  kosztu, edycja listy dotyka już tylko `items`/`itemIds`/`kayakIds`.
- Efekt uboczny (pożądany): `cancelReservation` sprawdza `costHours > 0` przed próbą zwrotu —
  skoro teraz zawsze 0 dla imprez klubowych, anulowanie nigdy nie tworzy wpisu "zwrócono/
  anulowano" w historii. Zero nowych wpisów w ogóle, zgodnie z życzeniem.
- `my_reservations_module.js`: usunięta przekreślona kwota "0 godz." przy tagu "Impreza
  klubowa" (kwota nie istnieje, nie ma czego przekreślać).
- Testy: `tests/test_bundle_reservations.py` zaktualizowane (stub + asercje `costHours=0`,
  `waived=False` zamiast `waived=True`) — 119/119. `functions` build+lint+vitest — 123/124 (ten
  sam 1 pre-istniejący fail, niezwiązany).

**Nie zweryfikowane live end-to-end** — w chwili wdrożenia użytkownik nie miał już aktywnego
statusu kierownika (impreza testowa widocznie zresetowana w międzyczasie). Wysoka pewność z
pełnego pokrycia testami, ale warto potwierdzić przy najbliższej realnej rezerwacji na imprezę:
historia godzinek powinna zostać BEZ ŻADNEGO nowego wpisu.

**Pozostałość do decyzji użytkownika**: w historii godzinek nadal widnieją 2 wpisy testowe z
tej sesji ("Impreza klubowa — Rezerwacja zestaw 02-10 do 04-10 26" / "04-09 do 06-09 26", oba
"anulowano 04-09-2026") — artefakty testów, nie mają wpływu na saldo, ale są szumem w historii.
Do usunięcia na żądanie (bezpośrednia edycja Firestore, nie przez UI).

## Runda 9 (05.09.2026) — wielu kierowników na jedną imprezę (z poziomu arkusza)

Feedback: zarząd chce móc z poziomu arkusza nadać uprawnienia kierownika kilku osobom naraz —
kolumna "Kierownik" ma dopuszczać kilka e-maili oddzielonych przecinkiem. Formularz zgłoszenia w
aplikacji bez zmian (nadal jeden e-mail, nadal wymagany dla organizatora Morzkulc) — arkusz jest
jedynym kanałem dopisywania KOLEJNYCH kierowników do już zgłoszonej/zatwierdzonej imprezy.

Zmiana modelu danych: `kierownikEmail`/`kierownikUid`/`kierownikDisplayName` (pojedyncze pola)
→ `kierownicy: {email, uid, displayName}[]` (pełna lista, także wpisy nierozwiązane — do wglądu)
+ `kierownikUids: string[]` (pochodna, tylko rozwiązane uid — wyłącznie do zapytań Firestore
`array-contains`, nowy indeks w `firestore.indexes.json`). `findActiveKierownikEvent`/
`findActiveKierownikConflict` (sygnatury bez zmian — nadal per-uid) i wszyscy ich konsumenci
(`registerUserHandler`, `gear_bundle_service`) działają bez modyfikacji.

- `events_service.ts`: nowy `resolveKierownicyList()` (parsuje CSV, rozwiązuje każdy e-mail
  niezależnie — błąd jednego nie blokuje reszty), `kierownikUidsOf()`.
- `eventsSyncFromSheet.ts`: parsuje kolumnę "Kierownik" jako listę; reguła "jeden kierownik
  naraz" sprawdzana PER KANDYDAT (tylko dla nowo dołączających do aktywnej imprezy) — konflikt
  wyklucza wyłącznie tego jednego, reszta listy syncuje się normalnie (poprawka względem rundy 1,
  gdzie konflikt blokował całe pole). `buildEventRowPatch` zapisuje e-maile z powrotem do arkusza
  jako listę oddzieloną przecinkiem.
- `adminApprovalHandler.ts`: konflikt "jeden kierownik naraz" sprawdzany dla KAŻDEGO kierownika
  z listy przy zatwierdzaniu.
- `onEventApproved.ts` + `eventsNotifyKierownik.ts`: mail "jesteś kierownikiem" wysyłany do
  KAŻDEGO nowego kierownika osobno (jobId per `eventId:uid`, nie per `eventId`) — obsługuje i
  pierwsze zatwierdzenie (wszyscy ówcześni kierownicy są "nowi"), i dopisanie kolejnej osoby do
  już zatwierdzonej imprezy przez arkusz (tylko dopisana osoba dostaje maila, reszta nie
  powtórnie). Maile informacyjne o nowej imprezie (`events.notifyNew`) — bez zmian, już szły do
  wszystkich.
- `getAdminPendingHandler.ts` / `admin_pending_module.js`: panel pokazuje wszystkich kierowników
  (dotąd jednego), etykieta "Kierownicy" gdy więcej niż jeden.

Zweryfikowane: `npm run build` + `npm run lint` + vitest (124 testy, 123 przechodzi — ten sam
1 pre-istniejący fail `boardDoesNotPay` sprzed tej zmiany, niezwiązany) + `pytest
tests/test_bundle_reservations.py` (119/119, bez zmian — mockuje `kierownik_events` na poziomie
`findActiveKierownikEvent`, nieczułe na zmianę modelu danych).

**Wymaga wdrożenia przed użyciem**: nowy indeks Firestore (`kierownikUids array-contains` +
`approved` + `endDate`) musi mieć status Enabled w konsoli przed deployem kodu. Po deployu
uruchomić ręcznie `events.syncFromSheet` (menu w arkuszu albo panel), żeby istniejące imprezy
klubowe (dziś jeszcze z samym `kierownikUid`) dostały nowe pole `kierownikUids` — do tego czasu
`findActiveKierownikEvent` nie znajdzie ich aktywnego kierownika (self-healing po pierwszym
syncu, ale bez ręcznego uruchomienia trzeba czekać na codzienny harmonogram).

**Nie zweryfikowane live** — brak realnej imprezy z dwoma kierownikami w tej sesji.

## Pliki zmienione (runda 2)

- `functions/src/modules/equipment/bundle/gear_bundle_service.ts` — `updateBundleReservationItems()`
- `functions/src/api/gearBundleReservationUpdateItemsHandler.ts` — nowy plik
- `functions/src/index.ts` — export `updateBundleGearReservationItems`
- `firebase.json` — rewrite `/api/gear/reservations/update-items`
- `public/modules/gear_module.js` — `renderClubEventBulkView` przebudowany (przyciski góra, edycja)
- `public/styles/gear.css` — `.clubEventActionsTop`
- `tests/test_bundle_reservations.py` — `TestScenarioClubEventItemsEdit` (8 testów)
