# Konsolidacja: jeden arkusz Setup + jeden folder na Dysku + wiadomości systemowe z arkusza — plan (17.08.2026)

> Status: PLAN ZATWIERDZONY przez użytkownika, wdrożenie w toku (Faza 0 — przygotowanie
> danych). Kontynuacja: `Audyty/17.08_kurs_wypożycza_PLAN_TO_DO.md`,
> `Audyty/17.08_audyt_kursant_wciaz_zablokowany.md`.

## Kontekst

Sesja audytu kursanta (17.08.2026) odkryła, że konfiguracja aplikacji jest rozproszona
po wielu arkuszach i zakładkach, część zmiennych nie ma w ogóle źródła w arkuszu
(edycja ręczna w konsoli Firestore albo — jak się dziś okazało — w ogóle nic), a treści
maili systemowych są zaszyte na sztywno w kodzie TypeScript — zarząd nie ma jednego
miejsca, żeby zobaczyć/zmienić ustawienia albo treść wiadomości bez pomocy programisty.
Użytkownik zdecydował o pełnej konsolidacji: **jeden nowy arkusz „App_SETUP" (utworzony 17.08.2026, id
`17hQBG_BBwzFf-tbhuKPzaAjEeP9d3pMIivQX_K-LnBA`)** na WSZYSTKIE
zmienne konfiguracyjne i WSZYSTKIE treści maili systemowych, oraz **jeden wspólny
folder na Dysku** — **„1_ZARZAD" > „APLIKACJA ARKUSZE"** (utworzony przez usera
17.08.2026, potwierdzone na Dysku: id folderu `1VFvWsHZgglD0Ih-1MlVU56WtXPL7rGxO`,
folder nadrzędny `1_ZARZAD` id `1k47fL4uy0ixSdSKsccAEsC-Ar8ur05ij`, puste, gotowe na
pliki) — na wszystkie arkusze używane przez aplikację, żeby zarząd wiedział, gdzie co
jest.

**Ograniczenie moich narzędzi (ważne dla podziału pracy):** nie mam dostępu do
tworzenia/przenoszenia plików i folderów na Dysku ani do edytora Apps Script (wklejanie
kodu do nowego bound-project). Mogę: czytać/wyszukiwać Dysk, pisać kod backendu,
przygotowywać dokładną treść nowych plików `.gs` i dokładne zestawienia danych do
wklejenia. **Fizyczne kroki na Dysku i w edytorze Apps Script wykonuje użytkownik**
według instrukcji krok po kroku.

## Zweryfikowany stan obecny (fakty z kodu i z żywych danych Firestore, nie domysły)

**5 dokumentów Firestore `setup/*`, z bardzo różnym stanem synchronizacji:**

| Dokument | Dziś synchronizowany z | Zakładka źródłowa | Stan na żywo (prod) |
|---|---|---|---|
| `setup/app` | arkusz członków (`cfg.sheets.membersSpreadsheetId`) | `APP_SETUP` | 20 modułów |
| `setup/vars_members` | arkusz członków | `SETUP` | 29 zmiennych |
| `setup/vars_gear` | osobny arkusz „Kajaki/Sprzęt" (`cfg.gear.kayaksSpreadsheetId`) | `SETUP` (ta sama nazwa zakładki, inny plik) | 27 zmiennych — **w tym 9 zmiennych `basen_*`, które tu NIE POWINNY być** (patrz niżej) |
| `setup/vars_basen` | **NIC** — zero writerów w `functions/src`/`appscript/` | brak | **dokument w ogóle nie istnieje na prod** |
| `setup/vars_godzinki` | **NIC** — jw. | brak | **dokument w ogóle nie istnieje na prod** |

**Nowo odkryty fakt (17.08, w trakcie przygotowań do tego planu):** `setup/vars_basen`
i `setup/vars_godzinki` nie istnieją w ogóle — `getBasenVars()`/`getGodzinkiVars()`
działają WYŁĄCZNIE na domyślnych wartościach zaszytych w kodzie
(`basen_service.ts:86-95`, `godzinki_vars.ts:26-27`). Co więcej, w `setup/vars_gear`
(czyli w SETUP arkusza sprzętu) ktoś już wcześniej ręcznie dopisał 9 zmiennych
`basen_*` (`basen_admin_mail`, `basen_cena_za_godzine`, `basen_cena_za_karnet`,
`basen_ile_wejsc_na_karnet`, `basen_limit_uczestników`, `basen_1_godzina_domyslna`,
`basen_2_godzina_domyslna`, `basen_sauna`, `basen_sauna_cena`,
`basen_rezygnacja_za_darmo`) — ale `getBasenVars()` czyta z `setup/vars_basen`, więc
**te wartości są martwe, nigdy nieużywane przez kod**. To osobny, realny bug —
ktoś próbował skonfigurować basen przez arkusz, myśląc że to działa, a nic się nie
działo. Konsolidacja do jednego arkusza Setup naprawia to przy okazji: te 9 wartości z
`vars_gear` posłuży jako gotowe dane wejściowe dla nowej zakładki `VARS_BASEN`
(prawdziwa, zamierzona konfiguracja zarządu — tylko w złym miejscu).

**9 miejsc z twardo zakodowaną treścią maili/wiadomości systemowych** (do zamiany na
szablony czytane z arkusza):
1. `service_config.ts` `welcomeBodyText()` (L162-240) — mail powitalny, 3 warianty wg
   dostępu do listy (`none`/`readonly`/`full`)
2. `usersSyncRolesFromSheet.ts` (~L241-267) — mail o zmianie roli (z warunkowym blokiem
   `listaInstructions`)
3. `eventsNotifyNew.ts` `buildNewEventEmail()` (L23-40) — nowa impreza
4. `eventsNotifyUpcoming.ts` `buildUpcomingEventEmail()` (L33-48) — zbliżająca się impreza
5. `adminNotifyPendingApprovals.ts` `buildPendingDigest()` (L61-110) — digest zaległości
   dla zarządu
6. `usersNotifyAkademikAccessChanged.ts` — nadanie/cofnięcie dostępu do akademika
7. `gearNotifyReservationCancelledByAdmin.ts` — anulowanie rezerwacji sprzętu przez admina
8. `basenNotifySessionCancelled.ts` — odwołanie zajęć na basenie
9. `godzinkiMonthlyBalanceReview.ts` — miesięczny przegląd bilansu godzinek

**Zakładka „Kurs" arkusza „Szkoleniówka"** (info o kursie: nazwa/daty/instruktor/link)
ma aktywny odczyt w apce (`getKursInfoHandler.ts` → Firestore `kurs_info`), ale task
`kursSyncFromSheet` który miałby ją syncować **nie ma żadnego triggera** — nie jest w
`APPS_SCRIPT_SYNC_ALLOWED_TASKS` (`index.ts:737-746`), nie ma pozycji w menu, nie ma
harmonogramu, nie ma przycisku w panelu zarządu. Zakładka „Co po kursie" (treści) ma
działający, ale osobny sync (`po_kursie_sync.gs` → `setup/kurs_po_kursie`, bezpośrednio
przez Firestore REST, nie przez backend).

**Kilometrówka** (`1XLat4KgFeE4AYrnPRdeW6n3zC7MByoBDlAfB7bOCyXw`) — w ogóle poza
`service_config.ts`, hardkodowana w `appscript/kilometrówka/env_config.gs:19,27`,
wszystkie zapisy bezpośrednio z Apps Script przez REST. Decyzja: tylko przenieść PLIK
do wspólnego folderu, bez zmian w kodzie/strukturze.

## Decyzje użytkownika (potwierdzone w tej sesji)

1. Do nowego arkusza Setup trafia **wszystkie 5** zmiennych setup (w tym domykamy lukę
   `vars_basen`/`vars_godzinki`, które dziś nie mają żadnego arkusza — okazało się, że
   nie mają nawet dokumentu w Firestore).
2. Do nowego arkusza Setup trafia **wszystkie 9** szablonów wiadomości na raz.
3. Zakładki „Kurs" i „Co po kursie" przenosimy do arkusza **członków** (nie do arkusza
   Setup — to treść/dane, nie konfiguracja) i **naprawiamy brakujący trigger** syncu
   „Kurs" przy okazji.
4. Kilometrówka: tylko przeniesienie pliku do wspólnego folderu, zero zmian w treści.
5. Arkusz „Szkoleniówka" po tej migracji jest całkowicie pusty z aktywnych zakładek —
   można go zarchiwizować (nie wchodzi do wspólnego folderu „arkusze używane w
   aplikacji", bo po migracji już nie jest używany).

## Docelowa struktura

### Arkusz „App_SETUP" (w folderze „1_ZARZAD" > „APLIKACJA ARKUSZE"), zakładki:
| Zakładka | → Firestore | Kolumny |
|---|---|---|
| `APP` | `setup/app.modules` | jak dziś `APP_SETUP` (Typ_elementu, Id_elementu, Nazwa_wyświetlana, Aktywny, ...) |
| `VARS_CZLONKOWIE` | `setup/vars_members` | jak dziś `SETUP` arkusza członków (Zmienna_nazwa, Wartość_zmiennej, Grupa zmiennych, Opis) |
| `VARS_SPRZET` | `setup/vars_gear` | jak dziś `SETUP` arkusza sprzętu, ten sam format — **bez 9 zmiennych `basen_*`, które przenoszą się do VARS_BASEN** |
| `VARS_BASEN` | `setup/vars_basen` (NOWY sync — dziś dokument nie istnieje) | ten sam format; 9 wierszy przeniesionych z dzisiejszego (błędnego) miejsca w `vars_gear`: `basen_admin_mail`, `basen_cena_za_godzine`, `basen_cena_za_karnet`, `basen_ile_wejsc_na_karnet`, `basen_limit_uczestników`, `basen_1_godzina_domyslna`, `basen_2_godzina_domyslna`, `basen_sauna`, `basen_sauna_cena`, `basen_rezygnacja_za_darmo` |
| `VARS_GODZINKI` | `setup/vars_godzinki` (NOWY sync — dziś dokument nie istnieje) | ten sam format; wiersze: `limit_ujemnego_salda` (domyślnie 20), `lata_waznosci` (domyślnie 4) — patrz `godzinki_vars.ts:12-27` |
| `MESSAGES` | `setup/messages` (NOWY dokument/kolekcja) | `id \| temat \| treść (z placeholderami {tak}) \| opis` — 9+ wierszy (część szablonów, np. powitalny, ma kilka wariantów → osobne `id` per wariant, np. `welcome_none`/`welcome_readonly`/`welcome_full`) |

### Arkusz członków — 2 nowe zakładki:
| Zakładka | → Firestore | Uwagi |
|---|---|---|
| `Kurs` | `kurs_info/{id}` | te same kolumny co dziś w Szkoleniówce (ID, Nazwa kursu, Data rozpoczęcia/zakończenia, Opis, Instruktor, Kontakt, Miejsce, Link, Aktywny?) |
| `Co po kursie` | `setup/kurs_po_kursie` | te same kolumny (tytuł, treść) |

### Wspólny folder Dysku „1_ZARZAD" > „APLIKACJA ARKUSZE" — pliki do przeniesienia:
Setup (nowy), arkusz członków, arkusz sprzętu/kajaki, arkusz godzinki-transition/archiwum
(`1dJpaV3OdFHo1ugD_NzSaysXbQyp_HkQe2EVpCiBVDac`), kilometrówka. Arkusz „Szkoleniówka"
**nie** wchodzi (po migracji nieużywany — do archiwizacji poza tym folderem).

## Fazy wdrożenia

### Faza 0 — Dysk i nowy arkusz (RĘCZNE, użytkownik; Claude przygotowuje dane)
1. ~~Utworzyć folder „1_ZARZAD" → podfolder „APLIKACJA ARKUSZE"~~ — **GOTOWE**,
   potwierdzone na Dysku 17.08.2026 (folder pusty, czeka na pliki).
2. ~~Utworzyć nowy Google Sheet w tym folderze~~ — **GOTOWE**: arkusz „App_SETUP"
   (id `17hQBG_BBwzFf-tbhuKPzaAjEeP9d3pMIivQX_K-LnBA`) utworzony 17.08.2026, dziś ma
   tylko domyślną pustą zakładkę „Arkusz1". **Zostaje:** dodać 6 zakładek z §„Docelowa
   struktura" (nagłówki kolumn dokładnie jak w tabeli wyżej) — patrz instrukcja niżej.
3. Utworzyć w arkuszu członków 2 nowe zakładki: `Kurs`, `Co po kursie`.
4. **Wypełnić nowe zakładki danymi PRZED zmianą kodu** (krytyczne dla bezpieczeństwa
   przejścia — patrz „Kolejność i ryzyko" niżej):
   - `APP`/`VARS_CZLONKOWIE`: skopiować jeden do jednego z dzisiejszych zakładek
     `APP_SETUP`/`SETUP` arkusza członków.
   - `VARS_SPRZET`: skopiować z dzisiejszej zakładki `SETUP` arkusza sprzętu — **bez**
     9 wierszy `basen_*` (te idą do VARS_BASEN).
   - `VARS_BASEN`/`VARS_GODZINKI`/`MESSAGES`: Claude przygotowuje gotowe zestawienia
     (żywe wartości z Firestore + wyekstrahowana treść maili) do wklejenia.
   - `Kurs`/`Co po kursie` w arkuszu członków: skopiować jeden do jednego z dzisiejszych
     zakładek arkusza Szkoleniówka.
5. Przenieść do folderu „1_ZARZAD" > „APLIKACJA ARKUSZE" istniejące pliki: arkusz członków, arkusz
   sprzętu, arkusz godzinki-transition, kilometrówka (samo przeniesienie pliku nie
   zmienia jego ID/URL — zero wpływu na działający kod).

### Faza 1 — Kod: jedno źródło zmiennych setup
1. `service_config.ts`: nowe pole `setup: {spreadsheetId}` (env `SVC_SETUP_SHEET_ID`,
   ustawiane w `.env.morzkulc-e9df7` / `.env.sprzet-skk-morzkulc` po utworzeniu arkusza
   w Fazie 0).
2. `setupSyncFromSheetTask.ts`: przepisać żeby czytał WSZYSTKIE 5 zakładek z JEDNEGO
   `cfg.setup.spreadsheetId` zamiast dzisiejszego rozbicia na `membersSheetId` (2
   zakładki) + `gearSheetId` (1 zakładka); dodać 2 nowe wywołania `readSetupVars()`
   (funkcja już istnieje, generyczna — reużyć bez zmian) dla `VARS_BASEN`→`setup/vars_basen`
   i `VARS_GODZINKI`→`setup/vars_godzinki`.
3. Nowy Apps Script projekt `appscript/setup/` (pliki analogiczne do
   `appscript/sprzęt/setup_sync.gs` + `ui_menu.gs`) — menu „sync setup" w nowym arkuszu.
4. Usunąć zdublowane pozycje menu „sync setup" z `appscript/członkowie sympatycy SKK/`
   i `appscript/sprzęt/` (od teraz setup żyje tylko w nowym arkuszu).

### Faza 2 — Kod: silnik szablonów wiadomości
1. Nowy helper (np. `functions/src/modules/setup/message_templates.ts`):
   `getMessageTemplate(db, id): Promise<{subject: string; body: string} | null>` +
   `renderTemplate(template, vars: Record<string,string>): string` (proste podstawianie
   `{placeholder}`).
2. Refaktor 9 miejsc z listy wyżej: każde czyta swój szablon po `id`, podstawia
   zmienne, **z fallbackiem do dzisiejszej hardkodowanej treści gdy wiersz w arkuszu
   nie istnieje** (bezpieczeństwo — brak wiersza nie wywala maila, tylko loguje
   ostrzeżenie, tak jak dziś `getKursWypozyczaFlag` domyślnie `false` gdy brak
   dokumentu).
3. Warianty (mail powitalny: 3 warianty, zmiana roli: fragment warunkowy
   `listaInstructions`) — każdy wariant jako osobny wiersz/`id` w zakładce `MESSAGES`,
   ŻEBY zarząd edytował gotowy tekst, a nie logikę warunkową.

### Faza 3 — Kod: „Kurs" + „Co po kursie" do arkusza członków, naprawa triggera
1. `kursSyncFromSheet.ts`: `spreadsheetId` z `cfg.kurs.spreadsheetId` →
   `cfg.sheets.membersSpreadsheetId`, `tabName` → `"Kurs"` (zakładka w arkuszu członków).
2. `po_kursie_sync.gs` → przenieść funkcję do Apps Script projektu arkusza członków
   (`appscript/członkowie sympatycy SKK/`), wskazać na nową zakładkę `Co po kursie`.
3. Dodać `"kursSyncFromSheet"` do `APPS_SCRIPT_SYNC_ALLOWED_TASKS` (`index.ts:737-746`)
   + pozycję menu „sync kurs" w arkuszu członków — **to naprawia realny, dziś
   istniejący bug (osierocona automatyzacja)**, niezależnie od reszty refaktoru.
4. Usunąć `appscript/kurs/` całkowicie, usunąć `cfg.kurs` z `service_config.ts`,
   `SVC_KURS_SHEET_ID`/`SVC_KURS_SHEET_TAB` z obu plików `.env.*`.

### Faza 4 — Przełączenie (kolejność krytyczna dla bezpieczeństwa)
1. Deploy kodu z Faz 1-3 (funkcje zaczynają czytać z nowych ścieżek).
2. **Dopiero teraz** odpalić nowy „sync setup" w nowym arkuszu Setup (Faza 0 już
   wypełniła go danymi identycznymi z dzisiejszymi — sync powinien być no-op / bez
   niespodzianek) oraz nowy „sync kurs" i „sync co po kursie" w arkuszu członków.
3. Zweryfikować (patrz „Weryfikacja") ZANIM usunie się cokolwiek ze starych arkuszy.
4. Dopiero po potwierdzeniu: usunąć stare zakładki `APP_SETUP`/`SETUP` z arkusza
   członków, `SETUP` z arkusza sprzętu, wszystkie zakładki arkusza Szkoleniówka (albo
   zarchiwizować cały plik).

## Kolejność i ryzyko

Największe ryzyko: jeśli kod zacznie czytać z nowego arkusza ZANIM ten arkusz zostanie
w pełni wypełniony poprawnymi danymi, pierwszy odpalony sync **nadpisze** dzisiejsze
działające `setup/app`, `setup/vars_members`, `setup/vars_gear` pustymi/niepełnymi
wartościami — realne ryzyko powtórki dzisiejszego typu buga (np. `kurs_wypożycza`
zniknie). Dlatego Faza 0 (wypełnienie nowego arkusza) musi zakończyć się PRZED Fazą 4
punkt 2 (pierwsze odpalenie nowego syncu), a stare zakładki kasujemy dopiero na samym
końcu, po pełnej weryfikacji.

## Weryfikacja

1. `npm --prefix functions run build && npm --prefix functions run lint`, testy Python.
2. Po Fazie 4.2: odczyt na żywo `setup/app`, `setup/vars_members`, `setup/vars_gear`,
   `setup/vars_basen`, `setup/vars_godzinki` — wartości identyczne z tymi sprzed
   migracji (ten sam wzorzec co dzisiejsze testy w tej sesji).
3. Test end-to-end bramki kursanta (jak dziś) — `kursWypozycza` nadal `true` dla
   obecnych kursantów po przejściu na nowy arkusz.
4. Dla każdego z 9 szablonów: wysłać/wywołać testowo i porównać treść z oczekiwaną
   (albo dry-run, gdzie taski to wspierają).
5. `getKursInfoHandler` zwraca dane z nowej zakładki `Kurs` po ręcznym odpaleniu
   naprawionego syncu.
6. Firebase deploy + weryfikacja `updateTime` przez gcloud (jak dotychczas w tej sesji).

## Zakres pracy — realistyczna skala

To NIE jest zmiana na jedną sesję: nowy arkusz z 6 zakładkami i ~60+ wierszami danych
do przepisania, 2 nowe zakładki w arkuszu członków, ~10 plików kodu backendu, nowy
projekt Apps Script, przeniesienie 4 plików na Dysku, i naprawa jednego realnego buga
(osierocony sync „Kurs") po drodze. Rekomendacja: wdrażać fazami (0→1→2→3→4) z
potwierdzeniem po każdej, tak jak dotychczasowe zmiany w tej sesji — nie na raz.

## Status realizacji

- [x] Plan zatwierdzony przez użytkownika (17.08.2026, Plan Mode)
- [x] Plan zapisany do repo (ten plik)
- [x] Faza 0 — dane przygotowane przez Claude (`Audyty/17.08_setup_dane_do_wklejenia.md`:
      VARS_BASEN, VARS_GODZINKI, 14 wierszy MESSAGES z placeholderami); folder/arkusz
      do utworzenia przez usera — NIE WYKONANE
- [ ] Faza 1 — kod: jedno źródło zmiennych setup
- [ ] Faza 2 — kod: silnik szablonów wiadomości
- [ ] Faza 3 — kod: Kurs + Co po kursie do arkusza członków, naprawa triggera
- [ ] Faza 4 — przełączenie i weryfikacja
