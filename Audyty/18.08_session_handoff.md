# Session Handoff — Pełne przełączenie na arkusz App_SETUP (18.08.2026)

## Where it started

Sesja zaczęła się od wczytania kontekstu projektu i audytów z 17.08 ("porządki w
setup"), po czym przerodziła się w pełny audyt gotowości nowego arkusza „App_SETUP" —
znaleziono, że nowa zakładka `Vars_KURS` nie ma miejsca docelowego w kodzie,
`Vars_GODZINKI` ma zmienne pod zupełnie innymi nazwami niż te, których szuka kod, a 2
klucze w `Vars_BASEN` nie pasują. Użytkownik zdecydował o pełnym przełączeniu: arkusz
App_SETUP ma być JEDYNYM źródłem konfiguracji, kod dostosowuje się do nazw w arkuszu
(nie odwrotnie, poza jednym wyjątkiem: `status_key`), plus zbudowanie 4 nowych funkcji
godzinkowych i zdedublowanie znalezionego hardkodu. Sesja zakończyła się pełnym
wdrożeniem na produkcję.

## Decisions locked + what shipped

- Arkusz App_SETUP = jedyne źródło modułów + wszystkich zmiennych Vars_* — WDROŻONE,
  real sync odpalony, dane zweryfikowane w Firestore.
- Kod dostosowany do nazw w arkuszu (nie arkusz do kodu) — `functions/src/modules/basen/basen_service.ts`
  (klucze `basen_limit_uczestników`/`basen_rezygnacja_za_darmo`), `functions/src/modules/hours/godzinki_vars.ts`
  (przepisany na `limit_debetu_godzinek`/`okres_wygasania_godzinek_mce` — **miesiące, nie lata**).
  Jedyny wyjątek: `status_key` w arkuszu poprawiony ręcznie (`status_zwieszony`→`status_zawieszony`)
  zamiast zmieniać kod — zbyt duży promień rażenia (pole w każdym `users_active/*`).
- `setup/vars_kurs` jako własny dokument (zamiast dokładania do `vars_members`) —
  przepięto 4 pliki: `functions/src/index.ts`, `functions/src/modules/equipment/bundle/gear_bundle_service.ts`,
  `functions/src/api/getKursantStatsHandler.ts`, `functions/src/modules/km/km_vars.ts`.
- 4 nowe funkcje godzinkowe zbudowane wg wyjaśnień użytkownika w rozmowie:
  limit 14 dni na zgłoszenie wstecz (`functions/src/api/submitGodzinkiHandler.ts`,
  helper `isTooOldGrantedAt`); auto-czyszczenie arkusza Google „Godzinki" — Firestore
  nietknięty (`functions/src/service/tasks/godzinkiArchiveSheetRows.ts`, nowa metoda
  `deleteRows()` w `functions/src/service/providers/googleSheetsProvider.ts`, nowy
  scheduler `godzinkiArchiveSheetDaily` w `functions/src/index.ts`); przelicznik PLN
  wykupu (`functions/src/api/godzinkiPurchaseHandler.ts` + digest w
  `functions/src/service/tasks/adminNotifyPendingApprovals.ts`); automatyczny bonus
  miesięczny zarząd/KR, idempotentny (`functions/src/service/tasks/godzinkiMonthlyBalanceReview.ts`,
  funkcja `creditBoardMonthlyBonus`).
- Zdedublowany hardkod (APP_URL x3, SVC_ADMIN_NOTIFY_*/SVC_ADMIN_ACTION_EMAIL env,
  OB_HOURS_EXPIRES_AT x2) → nowy `functions/src/modules/setup/app_vars.ts` (`getAppVars`),
  czytający nowe zmienne w arkuszu (`link_aplikacji`, `admin_notify_email`,
  `admin_notify_dni_progu`, `admin_action_email`, `data_wygasniecia_bilansu_otwarcia`).
  Pola `adminActionEmail`/`adminNotify` usunięte z `ServiceConfig`.
- `roleMappings` teraz z arkusza (nowa kolumna `Grupy_workspace` w zakładce `App_SETUP`,
  wypełniona przez Claude bezpośrednio przez Sheets API). `statusMappings` ZOSTAJE
  hardkodowany w `functions/src/service/tasks/setupSyncFromSheet.ts` — arkusz ma dziś
  `status_skreslony` zamiast `status_pending`, migracja odłożona.
- Nowy projekt Apps Script `appscript/setup/` (5 plików) — w repo, NIE wklejony do
  live edytora. Usunięto „sync setup" z menu + skasowano `setup_sync.gs` w
  `appscript/sprzęt/` i `appscript/członkowie sympatycy SKK/` — też tylko w repo, live
  edytory Apps Script wymagają ręcznej aktualizacji przez użytkownika.
- Gotcha znaleziona przy deployu: nowe pole `ServiceConfig` czytane z env potrzebuje
  hardkodowanego fallbacku (nie pusty string + throw) — `getServiceConfig()` woła się
  na module-load, a faza „discovery" `firebase deploy` nie ładuje `.env.<project>`.
  Naprawione w `functions/src/service/service_config.ts` (fallback = realne ID arkusza).
- `firebase deploy --only functions` — Deploy complete, 18.08.2026 ok. 12:08-12:09 UTC.
  Zweryfikowane przez gcloud: `SVC_SETUP_SHEET_ID` obecne w env żywych funkcji
  (`getSetup`, `appsScriptSync`, `godzinkiArchiveSheetDaily`, `submitGodzinki`,
  `purchaseGodzinki`), wszystkie ACTIVE ze świeżym `updateTime`.

## Key files for next session

- Audyt gotowości (pełna diagnoza sprzed zmian): `C:\Users\kswitek\Documents\morzkulc_app\Audyty\18.08_audyt_gotowosci_setup.md`
- Pierwotny plan konsolidacji (częściowo nieaktualny — Faza 1+ z niego wykonana inną
  ścieżką): `C:\Users\kswitek\Documents\morzkulc_app\Audyty\17.08_konsolidacja_setup_i_dysk_PLAN_TO_DO.md`
- Plan wykonania tej sesji (może już nie istnieć po `/clear`, treść zachowana w
  pamięci projektowej): `C:\Users\kswitek\.claude\plans\whimsical-snuggling-forest.md`
- Rdzeń zmian: `C:\Users\kswitek\Documents\morzkulc_app\functions\src\service\tasks\setupSyncFromSheet.ts`
  (przepisany silnik sync — czyta wszystkie 6 zakładek z jednego arkusza)
- `C:\Users\kswitek\Documents\morzkulc_app\functions\src\service\service_config.ts`
  (nowe `cfg.setup.spreadsheetId`, usunięte `adminActionEmail`/`adminNotify`)
- `C:\Users\kswitek\Documents\morzkulc_app\functions\.env.morzkulc-e9df7`
  (dodane `SVC_SETUP_SHEET_ID`, usunięte `SVC_ADMIN_NOTIFY_*`)
- Memory files touched:
  `C:\Users\kswitek\.claude\projects\C--Users-kswitek-Documents-morzkulc-app\memory\project_setup_konsolidacja.md` (przepisana)
  `C:\Users\kswitek\.claude\projects\C--Users-kswitek-Documents-morzkulc-app\memory\feedback_service_config_module_load.md` (nowa)
  `C:\Users\kswitek\.claude\projects\C--Users-kswitek-Documents-morzkulc-app\memory\feedback_dev_env_stale.md` (nowa)
  `C:\Users\kswitek\.claude\projects\C--Users-kswitek-Documents-morzkulc-app\memory\MEMORY.md` (indeks zaktualizowany)

## Running state

- Background processes: none
- Dev servers / ports: none
- Open worktrees / branches: none — wszystko na `master`, NIC nie zacommitowane
  (użytkownik zrobi commit sam)

## Verification — how to confirm things still work

- `npm --prefix functions run build && npm --prefix functions run lint` — oczekiwane: czysto
- `npm --prefix functions test` — oczekiwane: 105/106 (1 pre-istniejący, niezwiązany
  fail w `test/gear_core.test.ts`, patrz „Open questions")
- Żywa weryfikacja Firestore (wzorzec skryptu w `functions/scripts/`, `node --use-system-ca`
  + `admin.initializeApp({projectId:"morzkulc-e9df7"})`): `setup/app.roleMappings` ma 6
  ról z grupami, `setup/vars_basen`/`vars_godzinki`/`vars_kurs` istnieją i wypełnione
- gcloud: funkcje v2 w `morzkulc-e9df7/us-central1` mają `updateTime` z 18.08.2026 ok.
  12:08-12:09 UTC i `SVC_SETUP_SHEET_ID` w `serviceConfig.environmentVariables`

## Deferred + open questions

- Deferred: `Vars_MESSAGES` (silnik szablonów maili, Faza 2 pierwotnego planu z 17.08)
  — zakładka istnieje w arkuszu (audyt 18.08 znalazł zepsuty wiersz
  `akademik_granted/akademik_revoked` do naprawy), kod nadal ma treść maili
  zahardkodowaną w 9 miejscach.
- Deferred: zakładki „Kurs"/„Co po kursie" → arkusz członków (Faza 3 pierwotnego
  planu) — `kursSyncFromSheet` nadal bez triggera (osierocona automatyzacja).
- Deferred: 7 zmiennych „składki"/auto-zawieszenie w `Vars_CZLONKOWIE`
  (`semestr_1/2_start/stop`, `składka`, `zawieszenie_człoknostwa`, `Auto_zawieszenie_mcy`)
  — synchronizowane do Firestore, ale ZERO logiki konsumującej. To decyzja Claude
  (zbyt duże ryzyko automatycznego zawieszania członkostwa bez dopracowanej reguły),
  nie wprost potwierdzona przez użytkownika — do rewizji jeśli user chce inaczej.
- Open: pre-istniejący fail testu `test/gear_core.test.ts` → „zarząd/KR darmowo tylko
  przy boardDoesNotPay=true" (oczekiwano 0, jest 20). Potwierdzone `git status`, że ani
  `functions/src/modules/hours/hours_quote.ts` ani plik testu nie były tknięte tą
  sesją — bug/rozjazd testu sprzed tej sesji. Komentarz w kodzie mówi, że zwolnienie
  zarządu z opłaty świadomie przeniesiono do `gear_bundle_service.ts` (mechanizm
  "waived"), test tego nie odzwierciedla. Użytkownik jeszcze nie zdecydował, czy to
  badać.
- Open: pliki Apps Script (`appscript/setup/` nowy projekt + zaktualizowane
  `appscript/sprzęt/` i `appscript/członkowie sympatycy SKK/`) są gotowe w repo, ale
  NIE wklejone do live edytorów Apps Script — Claude nie ma tam dostępu (wymagałoby
  Apps Script API + domain-wide delegation ze scope `script.projects` + włączonego
  „Google Apps Script API" na koncie `admin@morzkulc.pl` — żadne z tego dziś nie jest
  skonfigurowane). Użytkownik pytał o te uprawnienia w tej sesji — odpowiedź udzielona
  w czacie, nic jeszcze nie skonfigurowane.

## Pick up here

Najbardziej prawdopodobna następna akcja: użytkownik ręcznie wkleja pliki z
`appscript/setup/` do nowego projektu Apps Script (bound do arkusza App_SETUP) i
aktualizuje dwa pozostałe edytory, potem odpala „sync setup" żeby potwierdzić działanie
z poziomu arkusza (nie tylko ze skryptu Claude).
